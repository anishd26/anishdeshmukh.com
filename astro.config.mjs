import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Your live site URL. Used for sitemaps and canonical links.
  site: 'https://anishdeshmukh.com',
  // No `base` is needed because you're using a custom domain at the root.
});
